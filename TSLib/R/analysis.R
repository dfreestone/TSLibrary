#  3.  cumulative_record: Create a cumulative record based on two events: the response such as
#          Lever_Press and the time marker (e.g. reinforcer, code 19).
#  8.  low_high_states.m: Estimate the index at which there is a change from a low state to
#         a high state (see help for information on criterium).
#
#  11. newrpm.m: Calculates event rate (such as responses per minute) as a function time since
#         a from to a to event for the whole time_event data input.
NULL
